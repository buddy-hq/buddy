# Bench Mode Implementation Log

> Historical note: this log tracks the earlier Bench implementation pass against `design.md`. The current post-refactor architecture is documented in `current-architecture.md`, and `bench-refactor.md` is the authoritative refactor and acceptance plan. Entries below are preserved for audit context and may describe code paths that have since been deleted.

This log tracks implementation work against `design.md`. Update it after every meaningful edit.

## 2026-06-16

- Started implementation pass from the locked design. No code edits yet.
- Added the first central Bench open-policy implementation in `packages/web/src/lib/bench-navigation.ts`: locked mode/default/policy types, surface defaults, target equality, preference helpers, a pure `resolveBenchOpenPolicy`, route-state parsing, `buildBenchNavigation`, and a request-shaped path through `useOpenBench`. Existing direct `openBench(directory, target, options)` remains as a compatibility route builder for this slice.
- Fixed the new navigation adapter to use the request directory when encoding TanStack route params.
- Reworked Bench auto-open wiring so detectors identify only policy id, event key, and target. `BenchAutoOpen` now calls `useOpenBench` with a `BenchOpenRequest` and `mode: "policy"` instead of directly navigating with caller-owned chat layout.
- Implemented the locked Explorer rule for concrete file opens: when a file-open plan resolves to the workspace panel while a Bench route is mounted, `useWorkspaceFileOpen` now opens the existing Bench `file` target through policy instead of queuing invisible right-sidebar state. Markdown opens in the same hook now also use the request-shaped Bench open API.
- Cleaned up the new path-segment helper after lint flagged the initial implementation.
- Fixed typecheck fallout from the first policy slice: the navigation adapter now consistently uses its `input.target`, and existing auto-open tests now assert the detector's reduced contract (`policyID`, `eventKey`, `target`) instead of caller-owned route/layout fields.
- Verification after this slice: `bun lint`, root `bun typecheck`, and `bun test --preload ./happydom.ts test/bench-open-policy.test.ts` from `packages/web` all passed.
- Added focused resolver tests in `packages/web/test/bench-navigation.test.ts` for locked surface defaults, target-default mode resolution, saved mode preference precedence, current live mode preservation while Bench is open, route-runtime geometry preservation, and auto-open blocking when another Bench target is active.
- Verification after adding resolver tests: `bun test --preload ./happydom.ts test/bench-open-policy.test.ts test/bench-navigation.test.ts` from `packages/web`, root `bun lint`, `git diff --check`, and root `bun typecheck` all passed.
- Started migrating remaining frontend Bench open call sites to the locked `BenchOpenRequest` shape. Converted Mermaid markdown fullscreen opens, workspace question-set panel opens, and workspace Mermaid panel opens to pass `{ directory, target, mode: "policy", autoOpen: null }` instead of caller-owned positional arguments.
- Migrated library panel Bench opens for flashcard decks, question sets, HTML widgets, figures, freeform figures, media presentations, and Mermaid diagrams to the locked request-shaped `useOpenBench` API.
- Migrated chat-renderer Bench opens for rendered figures, HTML widgets, question-set side panels, saved question-set tool cards, question-set author tasks, and flashcard author tasks to the locked request-shaped `useOpenBench` API. Removed the HTML-widget caller-owned chat-layout override so target policy owns mode selection.
- Completed the frontend caller migration: prompt Bench shortcuts, Mermaid tool cards, and media gallery opens now use `BenchOpenRequest` with `mode: "policy"`. Removed the positional `useOpenBench(directory, target, options)` compatibility overload and the old `openBench` route-builder wrapper, leaving `buildBenchNavigation` as the route adapter behind the locked request-shaped API.
- Verification after the full frontend open-call migration: root `bun lint`, `bun test --preload ./happydom.ts test/bench-open-policy.test.ts test/bench-navigation.test.ts` from `packages/web`, and root `bun typecheck` all passed.
- Began route-owned Bench runtime work. `DirectoryChatBenchPageLayout` no longer owns dock/floating mode or resets mode from a `benchKey`; it receives mode from the parent Bench route and reports user dock/float actions upward. The parent Bench route now parses current Bench target/mode from route state, owns live mode, writes user surface-family mode preferences, and replaces route search when the user docks or floats chat.
- Updated floating-chat layout tests to use a controlled mode wrapper, matching the new route-owned mode contract for `DirectoryChatBenchPageLayout`.
- Verification after route-owned mode work: `bun test --preload ./happydom.ts test/workspace-floating-chat.test.tsx test/bench-open-policy.test.ts test/bench-navigation.test.ts` from `packages/web`, root `bun lint`, `git diff --check`, and root `bun typecheck` all passed.
- Added the locked pixel-layout adapter `resolveBenchLayoutDefaults({ profile, viewport })` to `bench-navigation.ts`, with balanced and bench-first docked/floating numeric defaults from the design. `DirectoryChatBenchPageLayout` now consumes the route-selected layout profile for initial docked width, docked min/max constraints, Bench min width, floating default rect, floating min bounds, and resize/clamp behavior. Removed durable localStorage/persistent-panel geometry from the Bench layout path. Updated tests for the lower-right floating default and added direct layout-default assertions.
- Fixed the hook dependency warning introduced by profile-aware floating rect clamping.
- Verification after layout profile work: `bun test --preload ./happydom.ts test/workspace-floating-chat.test.tsx test/bench-open-policy.test.ts test/bench-navigation.test.ts` from `packages/web`, root `bun lint`, and root `bun typecheck` all passed.
- Added floating chat minimization as a user-only route-runtime substate in `DirectoryChatBenchPageLayout`. Minimize hides the floating window and shows a small restore affordance; restore brings the floating chat back; switching to docked clears minimization. Added a focused minimize/restore test.
- Adjusted the minimize/restore test to wait for the floating window exit/restore animation before asserting DOM removal/restoration.
- Changed the minimization render path to unmount the floating chat window immediately when minimized, leaving only the restore affordance. Normal floating-mode route transitions still use `AnimatePresence`.
- Changed restore affordance removal to be immediate when chat is restored, avoiding stale minimized controls during animation exit.
- Verification after floating minimization work: `bun test --preload ./happydom.ts test/workspace-floating-chat.test.tsx test/bench-open-policy.test.ts test/bench-navigation.test.ts` from `packages/web`, root `bun lint`, and root `bun typecheck` all passed.
- Added backend Bench context primitives: the canonical `bench_read_context` output schemas, an in-memory directory/session snapshot registry, `/api/bench/session/:sessionID/context` publish/read routes, and the `bench_read_context` and `bench_present` Buddy tools. Registered the new Bench feature on Buddy and Code Buddy so both tools are agent-visible through the normal feature system.
- Verification after the backend Bench tool/registry slice: root `bun lint` and root `bun typecheck` passed.
- Generated the SDK after adding the Bench context publish/read route.
- Added the frontend Bench route context publisher. The parent Bench route now owns a provider bridge, publishes `{ status: "closed" }` on parent unmount, serializes snapshot publishes, flushes the active provider before prompt submission, and provides a parent fallback snapshot while child routes are still loading.
- Added live `bench_read_context` providers for Markdown, reading, generic file preview, whiteboard, Mermaid, HTML widget, figure, freeform figure, media presentation, question set review, and flashcard review. Question-set context exposes submitted correctness/rationales only after result visibility; flashcard context exposes answer/back text only after reveal.
- Added Bench turn-prelude delivery in the backend prompt pipeline. The prelude reads the same synchronized registry snapshot as `bench_read_context`, emits only while Bench is open, skips duplicate generic Bench text for reading when the existing reading prelude is active, and stores current/delivered Bench fingerprints in teaching session state.
- Wired completed `bench_present` tool results into the frontend presenter: stable file/resource/whiteboard targets open through the locked `BenchOpenRequest` path with policy mode, and `close` leaves the parent Bench route. Historical presentation results are not replayed on initial message load.
- Added a central Bench leave-guard registry and wired it into target replacement, explicit close, and `bench_present` close. Markdown is the first guarded surface: it waits briefly for in-flight save, blocks conflict/saving failure states, attempts an autosave for dirty content, and blocks navigation if autosave fails.
- Verification after frontend context, prelude, presentation, and leave-guard work: root `bun lint`, root `bun typecheck`, and `bun test --preload ./happydom.ts test/workspace-floating-chat.test.tsx test/bench-open-policy.test.ts test/bench-navigation.test.ts test/bench-surface-render.test.tsx test/markdown-bench-page.test.ts` from `packages/web` passed.
- Backend focused verification passed with `bun test --preload ./test/preload.ts test/learning/runtime-tool-registration.test.ts test/learning/tool-schema-compatibility.test.ts test/learning/active-reading-context.test.ts` from `packages/buddy`.
- Completed the remaining in-scope design pass: added `classifyBenchTransition`, moved docked width, floating rectangle, and floating minimized/open state into the parent Bench route runtime, expanded `BenchRouteContextValue` to expose `state`, `setMode`, `setFloatingChatState`, `registerSurface`, and `flushContext`, and updated surface registration to include the active target.
- Tightened `bench_present` semantics by adding synchronized-snapshot preflight, `already_presenting` detection, and backend blocking for protected Markdown dirty/saving/conflict/save-error states before frontend navigation consumption. The frontend leave guard remains the final browser-owned navigation gate.
- Added focused tests for transition classification, blocked `bench_present` parsing, and backend `bench_present` presentation/already-presenting/dirty-blocked behavior.
- Faithfulness follow-up: closed Bench state is now explicitly published from the main chat route while Bench is not mounted, and missing backend snapshots now produce `sync_error` instead of being interpreted as closed. Frontend publishes are serialized per directory/session so a slower stale publish cannot overwrite a newer visible Bench state.
- Added a parent-route navigation blocker so route-origin Bench exits and target replacements pass through the same leave guard as explicit UI opens/closes and `bench_present`.
- Added static context providers for loading, error, and unavailable Bench route states so `bench_read_context` reflects what is visibly on Bench even before a live surface provider is ready.
- Tightened the remaining faithfulness issues from independent review: `bench_present` replacement opens now pass agent origin to the frontend leave guard, auto-open suppression is resolved in the central open helper, `leave-guard-blocked` is documented as an ignore policy outcome, and fallback/static loading-error snapshots derive required file/artifact/resource/tool refs from the active Bench target.
- Resolved the later faithfulness checks: removed the extra full `benchPresentation` payload from `bench_present` metadata, clarified that frontend leave guards use route-level `BenchTarget` while backend preflight uses synchronized `BenchContextTarget`, and made frontend prompt submission fail when no Bench context publisher is registered instead of silently omitting Bench truth.
- Addressed the first architecture review risk: `useRegisterBenchContextProvider` now registers a stable provider/guard wrapper and reads latest provider state through refs. Normal editor or review render churn no longer re-registers the surface or triggers a backend publish on every provider object change; prompt-time flush still reads the latest visible state.
- Split the overloaded `bench-navigation.ts` foundation into focused modules for targets, preferences, layout policy, open policy, route adapters, and the React open hook. The original file remains as a compatibility barrel. Added `known-issues.md` for the remaining architecture decisions that need dedicated design.
