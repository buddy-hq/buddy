# OpenCode subagent mode

Canonical root documentation: [OpenCode subagent runtime semantics](../../../architecture/decisions/opencode-subagent-runtime-semantics.md).

This quiz-folder note is a pointer only. The linked root document preserves the
three mode boundaries, permission-derived capabilities, and built-in/custom
comparison without making package documentation a substitute for a root
authority.
