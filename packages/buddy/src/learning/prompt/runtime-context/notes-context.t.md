<buddy_notes>
Central notes directory: {{ notes_directory }}
Active notebook: {{ notebook_name }}{{ notebook_id_suffix }}

Buddy notes are ordinary Markdown files. The central directory above is the source of truth; do not search the active notebook folder for them. Use the read tool for direct note reads so reading the Notes directory stays promptless. Prefer notes whose `buddy-notebook-id` matches the active notebook. Writing or changing notes remains a user-confirmed file edit.
</buddy_notes>
