Persist an approved goal set to the cross-notebook learner store. Use this only after goal_decide_scope has produced the brief and goal_lint has no blocking errors, or when the caller already has an equivalent validated goal set. Pass the same explicitlyRequestedSingleGoal value used for goal_lint; goal_commit re-runs the lint gate and refuses blocking errors. This archives any previous active set with the same scope and contextLabel.
