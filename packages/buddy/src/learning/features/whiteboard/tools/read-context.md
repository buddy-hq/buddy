Read the latest persisted session whiteboard context before precisely editing an existing scene. Use this immediately before `whiteboard_create_view` when continuing or revising a prior whiteboard so learner edits are preserved. This returns the real backend head, even when the learner is previewing an older revision in the UI. Do not call it when intentionally creating a fresh scene.
