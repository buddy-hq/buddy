/**
 * Rendered by every Bench route. The URL owns which target is active; BenchSurfaceHost owns the
 * mounted surface instances. Routes therefore render nothing at all.
 */
export function BenchTargetDeclaration() {
  return null
}
